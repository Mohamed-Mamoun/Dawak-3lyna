import 'package:flutter/material.dart';

class RequesrScreen extends StatelessWidget {
  const RequesrScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child:   Text('Requesr Screen'),
      ),
    );
  }
}


